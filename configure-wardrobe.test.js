const {combination} = require('./configure-wardrobe.js')

describe('combination', ()=>{
    describe('exact fit', () =>{
        
        let sum; 
        sum += combo[[i]];
      
      [sum == wall].forEach(combo[i]);{ //=> eigentlich müsste da noch ein => sein, aber zeigt mir dadurch ein Problem an?!
          it('should return sum = 250')
        };
    });
});
//wahrscheinlich total falsch
